# Copy this file to `.env` and adjust as needed.
# Postgres (the host is the docker-compose service name "postgres")
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=dlt
POSTGRES_USER=dlt
POSTGRES_PASSWORD=dlt

# Optional: tweak batch sizes, etc. (see dlt docs)
# DLT_PIPELINE__BATCH_SIZE=1000
